// Module 3 — Marché à 3 indices.
// Implémentation complète à l'étape 10 — stub neutre (boost ×1).

export function initM3(maintenant) {
  return { stub: true };
}

export function stepM3(m3, dt, simNow) {}

export function boostM3(m3) {
  return 1;
}
